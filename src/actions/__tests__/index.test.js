import { fetchArticles } from './../index';
import { FETCH_ARTICLES } from './../types'

describe('fetch articles', () => {
  it('has the correct type', () => {
    // const action = fetchArticles();
    // expect(action.type).toEqual(FETCH_ARTICLES);
  })

  it('has the correct payload', () => {
    // const action = fetchArticles('New articles');
    // expect(action.payload).toEqual('New articles');
  })
})